<div class="container-fluid">
  <h3><%= @topic.title %></h3>
</div>

<div class="container-fluid">  
  <%= link_to "Edit Topic", edit_topic_path(@topic) %> |
  <%= link_to "Delete Topic", @topic, method: :delete, data: { confirm: 'Are you sure you want to delete topic?' } %> |
  <%= link_to "New Bookmark", new_topic_bookmark_path(@topic) %> 
</div>


    
<div class="container-fluid">
  <div class="row">

    <div class="col-md-3">
      <div class="thumbnail">
      <%= @bookmarks.each do |bookmark| %>
      <% end %>
    </div>
  </div> 

    <div class="col-md-3">
      <div class="thumbnail">
      <%= @bookmarks.each do |bookmark| %>
      <% end %>
    </div>
    </div>
    
    <div class="col-md-3">
      <div class="thumbnail">
      <%= @bookmarks.each do |bookmark| %>
      <% end %>
    </div>
    </div>
    
    <div class="col-md-3">
      <div class="thumbnail">
      <%= @bookmarks.each do |bookmark| %>
      <% end %>
    </div>
    </div>

  </div>
</div>
    


<% if policy( @topic ).update? %>
  <%= link_to "Edit Topic", edit_topic_path, class: 'btn btn-success' %>
<% end %>



<% if policy( @topic ).destroy? %>
  <%= link_to "Delete Topic", @topic, method: :delete, class: 'btn btn-danger', date: { confirm: "Are you sure you want to delete this Topic?"} %>
<% end %>
  

</div>
